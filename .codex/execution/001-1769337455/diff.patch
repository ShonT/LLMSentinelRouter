diff --git a/docker-compose.yml b/docker-compose.yml
index 80a1498..5278990 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -40,11 +40,14 @@ services:
       - sentinelrouter_data:/home/sentinel/app/data
       - sentinelrouter_logs:/home/sentinel/app/logs
     restart: unless-stopped
-    # Resource limits as per design (1 CPU, 512MB RAM)
+    # Resource limits (increased for VECTORDB_LOCAL with transformers + onnxruntime)
     deploy:
       resources:
         limits:
-          cpus: '1.0'
+          cpus: '2.0'        # Increased for ONNX inference (CPU-intensive)
+          memory: 2G         # Increased for model loading + runtime (~1.5-2GB needed)
+        reservations:
+          cpus: '0.5'
           memory: 512M
     # Health check (using httpx which is in dependencies)
     healthcheck:
diff --git a/requirements.txt b/requirements.txt
index 2b03346..407a1c2 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -13,13 +13,13 @@ gunicorn==21.2.0
 networkx==3.1  # optional, for cycle detection
 
 # Optional for semantic processing (VECTORDB_LOCAL strategy)
-# onnxruntime==1.16.0  # Uncomment for local vector embeddings
-# transformers==4.36.0  # Uncomment for local vector embeddings
-# optimum[onnxruntime]==1.16.0  # Uncomment for model optimization
+onnxruntime==1.16.0  # Local vector embeddings
+transformers==4.36.0  # Local vector embeddings
+optimum[onnxruntime]==1.16.0  # Model optimization
 
 # Optional for vector storage (LanceDB)
-# lancedb==0.4.0  # Uncomment for persistent vector storage
-# pyarrow==14.0.0  # Uncomment for persistent vector storage
+lancedb==0.4.0  # Persistent vector storage
+pyarrow==14.0.0  # Persistent vector storage
 
 # Environment management
 python-dotenv==1.0.0
